apiVersion: v1
kind: Service
metadata:
  name: {{ include "jeanKluster.fullname" . }}
  labels:
    {{- include "jeanKluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jeanKluster.selectorLabels" . | nindent 4 }}
