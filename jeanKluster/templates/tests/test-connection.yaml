apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jeanKluster.fullname" . }}-test-connection"
  labels:
    {{- include "jeanKluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jeanKluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
